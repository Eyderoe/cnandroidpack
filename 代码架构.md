# ChartNavigation 项目架构

> 这是给人（维护者自己）看的文档，不是给 AI 的。
> 最后更新：2026-08-14。基于当前工作区代码编写；上次核对版本为 v2.1.3（提交 2918a3f）。

## 1. 这个项目是做什么的

航图浏览器：把本地 PDF 航图（命名如 `ZUCK-3P-01.pdf`）和实时模拟器/真实位置数据叠加显示，
画出本机与周围飞机的相对位置、高度差、航班号，支持 TCAS 距离过滤、地图仿射校准、明暗主题等。
定位是个人用的"可以取代 Navigraph"的工具。

## 2. 技术栈

- 语言/构建：C++23、CMake ≥ 3.28、Qt 6.9.3
- Qt 模块：Core / Gui / Widgets / Pdf / PdfWidgets / Concurrent / Positioning / Protobuf / ProtobufTools
- 第三方库：GeographicLib（WGS84 大地线）、Boost（asio/system，UDP）、QCoro6（协程缩略图）、
  nlohmann/json（Tmap/wtc.json）、Eigen（最小二乘）、doctest（单测）
- 平台：Windows（MinGW 13.x，CMake 里强制检查版本）、macOS、Linux、Android（arm64）

## 3. 顶层目录

```text
ChartNavigation/
├── CMakeLists.txt          # 主构建脚本（自动 MOC/RCC/UIC、protobuf、打包）
├── resources.qrc           # Qt 资源清单（图标/样式/翻译/wtc.json）
├── TODO                    # 维护者备忘：已知问题、近期/远期计划、版本规范
├── src/
│   ├── main.cpp            # 入口
│   ├── gui/                # 窗口级组件（main_window / main_widget / options / about / enroute / replay_control）
│   ├── ui/                 # 自绘控件（PdfView / StackedWidget / enhancedTree / statusBar / themeColor）
│   ├── services/           # 业务服务（DataProvider / SettingsManager）
│   ├── connector/          # 模拟器数据源适配层（xp / wlan / real / replay / msfs 空壳）
│   ├── utils/              # 工具（仿射、地理、字符串、高程、随机、事件回放、平台常量）
│   └── reserveCode/        # 保留实验代码（observe.hpp：脱离 Qt 的 QPointer 实现；typeErasure.hpp：类型擦除）
├── include/                # 第三方/自制单头文件（见 §6.5）
├── resources/              # 图片、qdarkstyle 样式、文档数据（wtc.json）
├── translation/            # 编译好的 .qm 翻译
├── example/                # 示例航图 + Tmap
├── test/                   # doctest 单元测试
├── android/                # Android 打包（Manifest + 图标）
└── .github/workflows/      # 三平台 CI 打包（手动触发）
```

## 4. 启动流程

```text
main()
 ├─ QApplication 基础设置（org/app 名、线程池、翻译）
 ├─ setLightTheme / setDarkTheme（qdarkstyle QSS 预加载）
 ├─ QSettings::setDefaultFormat(IniFormat)
 ├─ SettingsManager::instance()          # 单例启动，读入已存设置
 └─ main_window 构造
     ├─ StackedWidget（公共父节点）
     │   └─ 构造时创建 DataProvider（唯一的模拟器数据层，含录制/回放分支）
     ├─ main_widget（pdfBrowser） + enroute_widget 加入 StackedWidget
     ├─ 把 stackedWidget 的 DataProvider 注入两个页面
     ├─ initActionGroup（数据源/TCAS/符号 三个互斥动作组）
     ├─ 安卓特化：新增「权限 → 获取文件夹权限」菜单（SAF 授权）+ menu2ToolBar()（安卓无菜单栏）
     ├─ StatusBar（状态栏，1s 刷新）
     ├─ initConnect（动作与设置信号接线）
     └─ SettingsManager::broadcast()      # 把所有设置广播一遍，各控件据此初始化
```

### 4.1 关键类初始化顺序

**第一阶段：main()（基础设施，必须先于一切 UI）**

1. `QApplication`：org/app 名、`AA_DontShowIconsInMenus`。
2. 线程池：`QThreadPool::globalInstance()->setMaxThreadCount(max(2, 核心数))`（缩略图异步渲染用）。
3. 翻译：加载 `:/trans/translation/qtbase_zh_CN.qm`。
4. 主题预加载：`setLightTheme(&app)` / `setDarkTheme(&app)` —— 静态函数，把两套 QSS 读进静态变量；
   此时不决定用哪套（真正决定在 main_window 构造末尾）。
5. `QSettings::setDefaultFormat(IniFormat)`。
6. `SettingsManager::instance()`：**第一个被构造的关键类**——单例构造时把 QSettings 全部键读入
   `cache_const`；之后所有控件都从这里拿/写设置。
7. 应用图标 `:/icon/resources/navi.png`。
8. `main_window window;`

**第二阶段：main_window 构造（自顶向下建 UI 树）**

1. `ui->setupUi(this)`：建菜单栏/工具栏/状态栏等 .ui 静态控件（central 为空，页面手工建）。
2. `stackedWidget = new StackedWidget(this)` → 构造 `DataProvider`（**第二个关键类**，早于两个页面）：
   - `readTurbulenceCategory()`：读 `:/doc/resources/documents/wtc.json` 建尾流等级表；
   - `initConnect()`：订阅 settingChanged（dataSource / tcasRange / infoMode / showTrail / useCalGeoHeading）；
   - Debug 分支：读 `debugStoreData` / `debugReplayData`；录制时开 QFile 写临时目录回放文件；
     回放时强制 `dataSource=3`（replay）、建 `EventManage` 与回放控制窗 `replay_control` 并显示
     （见 §6.10）；
   - 高程：读 `globeFolder` 建 `NoaaGlobeView`（AGL 计算用）；
   - 读 `dataSource` 设置 → `make_unique<xpAdapter / wlanAdapter / realAdapter / replayAdapter>`；
   - `initSimulateDataConnect()`：注册 8 组 dataref（id/lat/lon/alt/trk/vs 各 64 项 + flightId/icao 各 512 项）
     + 连接回调（录制时顺带写 connectState 事件）；
   - 正常模式启动 `1000/infoFreq` ms（1Hz）刷新定时器；**回放模式不启动定时器**，改由 EventManage 驱动。
3. `pdfBrowser = new main_widget(this)`：
   - `ui->setupUi(this)` 建子控件，其中：
     - `PdfView` 构造：`initConnect()` 订阅 planeFollowed / isDarkTheme；`setPageMode(SinglePage)`、
       `setZoomMode(Custom)`、关滚动条；加载 `plane_small.png` / `plane_small_2.png`；
     - `Tree` 构造：定位 `CacheLocation` 缓存目录（>1000 项则标记启动时清空）、订阅
       itemExpanded / itemCollapsed / itemPressed（右键高亮）；
   - 创建 `QPdfDocument` 并 `setDocument` 给 PdfView；
   - `main_widget::initConnect()`：滑条范围 [20,400]、滑条↔滚轮双向联动（blockSignals 防回环）、
     订阅 scaleBarEnable / spliterSta；
   - `initFileTree()`：读 `chartFolder` 填充文件夹下拉框。
4. `enroute = new enroute_widget(this)`：纯 `setupUi`，空壳。
5. `stackedWidget->addWidget(pdfBrowser / enroute)` → `setCentralWidget(stackedWidget)`。
6. **注入 DataProvider**：`pdfBrowser->setDataProvider(...)` → `PdfView::setDataProvider`（存指针 +
   connect(dataUpdated → onDataUpdated)）；`enroute->setDataProvider(...)`（只存指针）。
7. `initActionGroup()`：按 objectName 建 source / tcas / symbol 三个互斥 QActionGroup。
8. 安卓特化：加「权限 → 获取文件夹权限」菜单 + `menu2toolBar()`（菜单栏转工具栏）。
9. `new StatusBar(ui->statusbar)`：三个标签 + 分隔线、1s 刷新定时器、订阅
   dataSource / affineError / affineQuality / simuConnect / latitu / longitu / altRelat。
10. `initConnect()`：main_window 订阅全部 ConstKey（窗口几何/状态、动作勾选、置顶）与 TempKey
    （isDarkTheme），并连接所有 QAction（含新加的动作）。
11. `SettingsManager::broadcast()` —— **第二波初始化（依赖"先连接、后广播"的时序）**，每个
    ConstKey 按默认值广播一次，各订阅者此时才拿到初始值：
    - DataProvider：收到 dataSource 再次 `setConnector()`（与构造时读设置重叠，重建适配器，幂等）；
      tcasRange / infoMode / showTrail / useCalGeoHeading 写入缓存；
    - PdfView：planeFollowed → `setCenterOn`；
    - main_widget：scaleBarEnable → 滑条显隐；spliterSta → `restoreState` 恢复分割条；
    - StatusBar：dataSource → 数据源标签；
    - main_window：`restoreGeometry / restoreState`、动作勾选、置顶窗口标志。
12. `isDarkTheme` 按系统 `colorScheme()` 写入 SettingsManager → TempKey 信号 → `main_window::setTheme`
    （应用 QSS）与 `PdfView::setColorTheme`。

**第三阶段：事件循环**

`window.show()`（安卓 `showMaximized`）→ `exec()`。此后全是事件驱动：
文件树双击 → `main_widget::loadPdfFile` → `loadPdfFileMapping` → 翻页 → `loadPdfPageMapping` →
`PdfView::loadMappingData`；模拟器数据 → `DataProvider::simulateDataUpdate` → `processDataFrame` →
`dataUpdated` → PdfView 重绘；回放模式 → `EventManage::eventReady` → `replayDataUpdate` → 同一路径。

**关键依赖与时序坑**

- `SettingsManager` 必须最先构造（main 里显式 `instance()`），因为 PdfView / main_widget / Tree /
  DataProvider / StatusBar 的构造都会访问它。
- `DataProvider` 在 StackedWidget 构造时创建，早于两个页面；页面只能通过 setter 注入拿到指针，
  不能反向找父（约定见 §7.1）。
- `PdfView::setDataProvider` 在构造之后才被调用，因此构造期间 `dataProvider` 为空，
  必须等注入后才能收到 `dataUpdated`。
- `broadcast()` 是各控件初始化的真正驱动点，必须在整棵 UI 树和所有订阅者就位后调用。

## 5. UI 层级结构

```text
main_window (QMainWindow)
├── 菜单栏 / 工具栏 / 状态栏(StatusBar)
└── central: StackedWidget              ← 页面容器 + DataProvider 持有者
    ├── 页0: main_widget                ← 航图页（默认页）
    │   └── QSplitter
    │       ├── 左: 文件夹下拉框 + 页码 + Tree(文件树/缩略图)
    │       └── 右: PdfView + 缩放滑条
    └── 页1: enroute_widget             ← 航路页（目前是空壳，已预留 DataProvider 注入）
```

`StackedWidget` 是两页的公共父节点，因此持有共享的 `DataProvider`；页面通过 setter 注入拿到它，
子控件**不允许**反向查找父对象（这是本项目的明确约定，见 §7.1）。

回放模式下 `DataProvider` 还会以 StackedWidget 为父、`Qt::Window` 标志新建一个独立的
`replay_control` 控制窗（见 §6.10），不属于两页。

## 6. 核心模块职责

### 6.1 services/DataProvider —— 唯一的模拟器数据层

- 在 `StackedWidget` 构造时创建，`main_window` 负责注入给 `pdfBrowser` 和 `enroute`。
- 持有 `InterfaceSimu` 适配器，按设置 `dataSource` 选择 xplane / wlan / real / replay（回放模式强制
  replay），监听设置变化自动切换。
- 正常模式：定时器（间隔 `1000/infoFreq` ms，`infoFreq` 当前固定 1Hz）拉取 8 组数据：
  id / lat / lon / alt / trk / vs（64 项数组）+ flightId / icao（512 项字符数组，每架飞机 8 字节）。
- 每帧统一走 `processDataFrame()`：更新航班轨迹 → 写 `latitu/longitu` → 用 `NoaaGlobeView` 查本机
  地面高程算 AGL（非安卓，`altRelat`；无高程或海洋为 -500，负值钳 0；安卓直接放 `planeAlt*ft`）→
  发射 `dataUpdated()` 信号，`PdfView` 等消费者靠信号刷新（推，不是轮询）。
- 连接状态写入 `simuConnect`，对外提供 `isConnected()` / `closeSimu()`。
- 对外还暴露 8 组数组 getter、`getAvailableNum()`、`getAlt()`（查 GLOBE 高程）。
- **显示状态也归数据层管**：tcasRange / infoMode / showTrail / useCalGeoHeading 四个设置由 DataProvider
  订阅并缓存，对外 `getTcasMode()` / `getInfoMode()` / `getShowTrail()` / `getUseCalGeo()`（2026-08 从
  PdfView 迁入，显示行为以数据层为准）。
- **航班轨迹**：每次刷新按航班号（`slice(multiFlightIdVal, idx)`）为每架非空飞机把经纬点记入
  `trails`（`std::map<std::string, AircraftTrail>`，实现见 §6.8）；map 达到 128 个航班后，
  一次性清空本轮已消失航班的轨迹。对外提供 `getGroundSpeed()` / `getGeoHeading()` / `getPoints()`。
- 尾流等级：构造时读 `:/doc/resources/documents/wtc.json`（ICAO → 首字母），
  对外 `getWakeCategory(const std::string &)`；查不到返回 `' '`（空格）。这是从 StackedWidget 迁过来的（2026-08 重构）。
- **Debug 录制/回放**：`debugStoreData` 把每帧 8 组数组 + 连接事件写成回放文件；`debugReplayData`
  用 `EventManage` 按事件时间戳回放，走与实时相同的 `processDataFrame()`；回放时自动创建并显示
  `replay_control` 控制窗（已接线），进度信号带毫秒时间戳。完整逻辑见 §6.10。
- `slice<Str>()`（dataProvider.hpp 里的模板自由函数）：把 512 项数组按索引切成最多 8 字符的
  `QString` / `std::string`，用于航班号 / ICAO 解码。

### 6.2 ui/PdfView —— 航图显示与飞机绘制

- 继承 `QPdfView`。绘制顺序：先画 PDF，再叠加飞机。
- `loadMappingData()`：由 `main_widget` 传入本页映射点 + 旋转角 + 阈值，交给 `AffineTransformer`。
- `paintEvent()`：暗色主题用 `CompositionMode_Difference` 反色；只有
  `已连接 && 飞机贴图可用 && 仿射变换可用` 才画飞机。
- `drawPlane()`：自身画 `plane_small.png`，其他飞机画 `plane_small_2.png` 并附带
  航班信息（TCAS 过滤抽成 `whetherShow()`，高度差描述抽成 `altInfo()`）：
  - TCAS 过滤：nm30（30NM 且高度差 ≤ 9900ft）/ nm6（6NM 且高度差 ≤ 1200ft）/
    none（全隐藏）/ all（全显示）；范围模式取自 `DataProvider::getTcasMode()`。
  - 信息文本在 `zoomFactor() > 0.45` 才画，按 `InfoMode` 分级（`DataProvider::getInfoMode()`）：
    base = 相对高度趋势（±xx + ↑/↓）；extend = 再加航班号；full = 再加「地速(节) 尾流等级」。
    尾流等级来自 `getWakeCategory(icao)`（查不到时回退显示机型码 `(ICAO)`；PdfView 内有静态缓存）。
  - 计算航向：`useCalGeoHeading` 开启时用 `DataProvider::getGeoHeading()` 覆盖 `trk`
    （轨迹不足时回退导航数据）。
  - 航迹：`showTrail` 开启时从 `DataProvider::getPoints()` 取轨迹，等间隔采样最多 8 个点画橙色圆点，
    透明度逐点递减（最新点最实），小缩放（<0.45）时半径 2 否则 3。
- `onDataUpdated()`：收到 `DataProvider::dataUpdated` 后刷新视口；若 `planeFollowed` 开启且没在拖拽，
  把自身飞机保持在视口内（边缘 10px 容差）。
- 缩放：滚轮 ×1.2/0.8，范围 [0.2, 4]，以鼠标为中心缩放，发 `zoomFactor_changed` 联动缩放滑条。
- 旧版自带整套模拟器代码（connector/定时器/数组），2026-08 重构后全部移除，只依赖 `DataProvider`。

### 6.3 gui/main_widget —— 航图页

- 负责 PDF 文档生命周期（QPdfDocument）、文件树、页码、缩放滑条。
- `loadFolder()`：把某个文件夹直接灌进文件树（"打开文件夹"动作/文件夹下拉框切换用）。
- `loadPdfFile()`：文件名 → `baseName.left(4)` 得到 ICAO →
  到设置 `dataFolder` 找 `<ICAO>.Tmap` → 按文件基本名（如 `ZUCK-3P-01`）取页配置。
- `.Tmap` 是 JSON：顶层对象键为航图基本名，值是页面数组；每页 `[{page,type,rotate}, {lat,lon,x,y}...]`。
  `type=="parking"` 时阈值用 10，否则 5。
- 页码切换 → 取该页映射 → `pdf_widget->loadMappingData()`。

### 6.4 ui/enhancedTree —— 文件树 + 缩略图

- `Tree`（QTreeWidget）递归遍历文件夹；`Node` 按类型着色（文件夹蓝 / PDF 橙 / 其他绿）。
- 缩略图：`QPdfDocument` 渲染首页 + `QCoro::Task` 异步，按设置 `showThumb` 控制。
- 用 rapidhash 对文件内容哈希后缓存到 `QStandardPaths::CacheLocation`，缓存文件 >1000 时下次启动清空。
- 右键节点切换红/原色高亮（标记用）；`onlyDisplayPdf` 设置过滤非 PDF。

### 6.5 connector/ —— 数据源适配层

统一接口 `InterfaceSimu`：
`setCallback(bool 连接状态)` / `close()` / `addDatarefArray(name, freq)` / `getDataref(idx, span, default)`。

| 适配器 | 数据来源 | 说明 |
| --- | --- | --- |
| `xpAdapter` | X-Plane UDP（`include/XPlaneUDP.hpp`，自制的 boost::asio 客户端） | TCAS 相关 dataref：`sim/cockpit2/tcas/targets/...`，含 `icao_type`（icao）；⚠ RREF 单客户端约 512 订阅上限，8 组全订会丢掉后面的数组，见 §13.2 |
| `wlanAdapter` | 局域网多播 `239.255.73.16:57316`，protobuf 协议 | `plane.proto` 生成 `plane.qpb.*`；flightId=7、icao=8；id 存为 `id+1`（0 表示无飞机） |
| `realAdapter` | Qt Positioning 定位（GPS） | 只有本机，`trk/alt` 视坐标类型而定；trk 为 NaN 时兜底为 0 |
| `replayAdapter` | 回放模式专用 | 空壳（不真正连数据源），保证回放不被适配器影响；`SimulatorSource::replay` 由 DataProvider 在回放模式强制写入 dataSource |
| `msfs` | — | 空壳，未实现 |

`SimulatorSource` 枚举（xplane/wlan/real/replay）在 `allAdapter.hpp`。

> ⚠ xp/wlan 两个适配器各自用一个 `std::thread` 跑 asio `io_context`，且 worker 线程内会碰 Qt
> （回调里的 qDebug/QSettings/QDateTime，wlan 还有 QProtobufSerializer）。
> 这在 Windows MinGW + Qt 6.9 下会在线程退出时触发 Qt6Core 的 TLS 清理崩溃——详见 §13.1。

### 6.6 services/SettingsManager —— 设置单例（全局信号总线）

- 单例；`ConstKey` 持久化到 QSettings（ini），`TempKey` 只在运行时存在。
- `set()` 写缓存 + 发 `settingChanged` 信号（重载：ConstKey / TempKey），`get()` 懒加载默认值。
- `broadcast()`：启动时把所有键按默认值发一遍，是各控件初始化的驱动方式。
- 安卓特化：写设置时直接落盘 + sync（安卓退出机制不同）。
- 同文件里的 `CycleIdx` 用于循环切换（主页面 2 页；旋转角 {0,270,180,90}）。

### 6.7 ui/statusBar —— 状态栏

三个标签（模拟器在线/离线、经纬+AGL、仿射误差/质量），1s 定时器刷新，
数据来自 SettingsManager 的 ConstKey `dataSource` + TempKey 信号（affineError/Quality、simuConnect、
latitu/longitu、altRelat），
但**信号只置脏标记（updateSimu/updatePlane/updateAffine），真正改标签由 1s 定时器统一消费**
——高频信号被合并成 1Hz 刷新，避免狂刷 QLabel（见 §7.5）。
非安卓显示 AGL（高程算的离地高），安卓显示绝对高度 Alt；回放数据源时标签显示「回放 在线/离线」。

### 6.8 utils/ —— 工具

| 文件 | 职责 |
| --- | --- |
| `affineTransformer` | 仿射变换：RANSAC 剔除离群点（≤14 点穷举 C(n,3)，否则随机 200 组）+ Eigen 最小二乘；提供误差/质量评估 |
| `geographic` | 距离（简单等距近似 + 几何距离）、方位/距离推点（GeographicLib WGS84）、`bearingSimple` 相对真航向；另有 `AircraftTrail` 航班轨迹类（至多存 1 分钟：`calculateGroundSpeed()` 取最新一半轨迹采样 ≤10 点求平均地速，`calculateGeoHeading()` 取最新 ≤5 段航向按圆周平均） |
| `noaaGlobe` | GLoBE 30 弧秒高程瓦片，`NoaaGlobeView` 用内存映射（mio）；已接入 DataProvider 算 AGL（§6.1），也有单测 |
| `eventManage` | 回放文件解析 + 离散事件调度器（参考 ns3）：单发 QTimer 按事件相对时间逐个补发 `eventReady`，支持 seek/step/进度（§6.10） |
| `stringProcess` | join / split / toHex |
| `randomGen` | 随机整数（RANSAC 采样用） |
| `constValue` | 平台枚举与宏检测、单位常量（m2ft / ft2m / nm2m / m2nm）、`NaN` |

### 6.9 其他

- `themeColor`：加载 qdarkstyle QSS（暗/亮两套，资源前缀 `/css`）；Windows 暗色主题未完全解决（代码有 TODO 注释）。
- `options_widget`：设置页（航图文件夹、映射文件夹、地形数据文件夹、只显示 PDF），保存窗口位置。
- `about_dialog`：致谢弹窗。
- `reserveCode/`：实验代码，未在实际逻辑中使用——`observe.hpp`（不依赖 Qt 的弱指针包装 `SafePtr`）、
  `typeErasure.hpp`（concept + 类型擦除示例，从 interface.hpp 迁出）。

### 6.10 回放数据（Debug 用）—— 录制 + 离散事件回放

参考 ns3 离散事件模拟器的设计，全部由 ConstKey 控制（`debugStoreData` / `debugReplayData` /
`debugReplayFile`），是 **Debug 功能，没有正式菜单入口**（只能改 ini）；但回放模式下会自动弹出
`replay_control` 控制窗。

**录制（debugStoreData）**

- 开启后（与回放互斥：`debugStoreData && !debugReplayData`），在系统临时目录建
  `ChartNavigation-<时间戳>.txt`，先写文件头 `offsetTimeMs,0` 和一行 freq，然后：
  - 每帧 `simulateDataUpdate()` 拉完 8 组数组后，写一条 `data` 记录：`<相对ms>,data,<字节数>` 头 +
    `qCompress` 后的 QDataStream 字节（相对时间 + "data" + 1408 个 float：64×6 + 512×2）；
  - 连接状态变化（connector 回调）时写一条 `connectState` 记录（`true\n` / `false\n`）。

**回放（debugReplayData + debugReplayFile）**

- DataProvider 构造时若开启回放：**不启动轮询定时器、也不拉取适配器**，改为创建 `EventManage` 读文件，
  把每条记录解析成 `Event{type, time, payload}`（connectState 存 bool；data 存解压后的字节）。
- `EventManage` 是"单发 QTimer 驱动的离散事件调度器"：`start()` 后按事件相对时间逐个
  `fireCurrent()`（迟到的立即补发），每个事件发 `eventReady(event)`；播完发 `finished`。
- DataProvider 收到事件：`connectState` → `setConnectState`；`simulateData` → `replayDataUpdate`
  （把字节流反解回 8 组数组）→ 走与实时**同一个** `processDataFrame()`（轨迹/经纬/AGL/dataUpdated）。
- 跳转：`EventManage::seekToEvent / seekTimeMs / seekPercent / stepEvents / stepPercent`——跳转后立即
  补发当前事件，并把 `baseMs` 重新锚定到现在（后续按事件相对时间差继续）；DataProvider 包装成
  `replayStepEvents / replayStepPercent / replaySeekPercent` 对外，另有 `replayEventCount()` /
  `isReplayMode()`；`EventManage::progressChanged(qint64 毫秒)` 直接转发为
  `DataProvider::replayProgressChanged(qint64)`。
- `replay_control`（gui/，v2.1.3 起接入）：滑条（0~999，拖拽发 `value*100/999` 的 seekPercent）+ 四个
  按钮（±10 事件、±1%），发 `seekPercentRequested / stepEventsRequested / stepPercentRequested`；
  `setDuration / setPosition` 反向驱动滑条。**只在回放模式创建**：DataProvider 构造时
  `new replay_control(qobject_cast<QWidget*>(parent))` → `setWindowFlag(Qt::Window)` →
  连接三个 Requested 信号 → `replayProgressChanged → setPosition` → `show()`。

## 7. 核心数据流

### 7.1 模拟器数据（推模式）

```text
连接器(InterfaceSimu) ──定时器(1/infoFreq s)──> DataProvider
    │                                 │ 写入 SettingsManager(lat/lon、altRelat(AGL)、simuConnect)
    │                                 │ 按航班号记录轨迹（trails）
    │                                 └─ dataUpdated() 信号
    ▼                                      ▼
  （设置 dataSource 变化时切换）      PdfView::onDataUpdated → 重绘/居中
                                     （未来 enroute_widget 也订阅同一个信号）
```

约定：数据与显示状态（TCAS 范围/信息模式/航迹/计算航向）统一由 DataProvider 向下提供，
UI 组件不反向找父对象；两个页面共享同一份数据。

### 7.1b 回放数据流

```text
EventManage（单发 QTimer 按事件时间戳调度）
   ├─ eventReady(Event) ──> DataProvider::replayDataUpdate ──> processDataFrame()（与实时同一路径）
   │                          └─ 与实时一样发 dataUpdated() → PdfView 重绘
   ├─ progressChanged(qint64 ms) ──> DataProvider::replayProgressChanged ──> replay_control::setPosition（滑条）
   └─ finished ──> setConnectState(false)

跳转/步进：replay_control（回放模式自动弹出，已接线）
   ├─ 滑条/按钮 ──> seekPercentRequested / stepEventsRequested / stepPercentRequested
   └─> DataProvider::replaySeekPercent / replayStepEvents / replayStepPercent
       ──> EventManage::seekPercent / stepEvents / stepPercent ──> 立即补发当前事件 + 重新锚定 baseMs
```

录制时则相反：`simulateDataUpdate()` 每帧把 8 组数组压缩成 `data` 事件追加写盘，连接变化写
`connectState` 事件（文件格式见 §9）。

### 7.2 航图加载与映射

```text
双击文件树 ──> main_widget::loadPdfFile(path)
   ├─ QPdfDocument 加载 PDF
   ├─ ICAO = 文件名前4字符
   └─ dataFolder/<ICAO>.Tmap → 按基本名取该航图 → 页面数组
翻页 ──> loadPdfPageMapping(page) ──> {映射点, 旋转角, 阈值}
   └─> PdfView::loadMappingData ──> AffineTransformer(拟合+评估) ──> 绘制时 trans() 经纬→屏幕
```

### 7.3 设置流

```text
任意控件 SettingsManager::set(key, value)
   └─ settingChanged(key, value) 信号
        ├─ DataProvider → 切换连接器（dataSource）/ TCAS 范围 / 信息模式 / 航迹 / 计算航向
        ├─ PdfView → 跟随 / 主题
        ├─ StatusBar → 刷新标签
        ├─ Tree → 缩略图开关 / 主题
        └─ main_window → 窗口状态 / 动作勾选
```

### 7.4 缩放条 ↔ 鼠标滚轮联动

两条单向通路 + 一个防回环开关，桥接都在 `main_widget`。

```text
滑条 valueChanged(int) ──> setZoomFactor(value/100.0)        （滑条 → PDF）
滚轮 wheelEvent ──> emit zoomFactor_changed(double) ──> 滑条 setValue
                       （blockSignals 挡住 valueChanged，避免回环）
```

关键点：

- 滑条范围 `[zoomMin*100, zoomMax*100]`（即 20~400，对应 0.2x~4x），初始 100；
  `zoomMin/zoomMax` 定义在 `pdfView.hpp`。
- `zoomFactor_changed` 是 PdfView 自定义信号，**只在滚轮事件里发**；
  `main_widget` 收到后写滑条时先 `blockSignals(true)`，写完再放开，
  防止 `setValue → valueChanged → setZoomFactor` 的冗余回环。
- 滚轮缩放还带"以鼠标为中心"的滚动条补偿（wheelEvent 里按新旧缩放与鼠标位置重算 scroll bar），与滑条无关。
- 曾经的空 `on_scale_verticalSlider_valueChanged(int)` 槽（会被 Qt 自动连接机制重复接线）已在
  v2.1.3 删除，现在是纯 lambda 接线。

### 7.5 数据刷新机制：轮询 vs 信号槽

**真正靠轮询（定时器主动拉）的只有两处：**

1. `DataProvider` 的模拟器拉取：`simuUpdateTimer`（1000/infoFreq，1Hz）→ `simulateDataUpdate()` →
   `connector->getDataref(...)` 从适配器内部数组拷 8 组数据。这是唯一"定时主动拉"的数据源头。
2. `StatusBar` 的合并刷新：1s 定时器 `update()` 消费 `updateSimu / updatePlane / updateAffine` 三个
   脏标记——上游信号只置位，真正改标签靠这个定时器（高频信号合并成 1Hz，避免狂刷 QLabel）。

（XPlaneUDP / wlanUdp 后台线程里 asio 协程收 UDP 包 → 写内部数组，是网络事件驱动而非轮询；
但 DataProvider 取数是轮询。）

**其余全部是信号槽/回调（推模式）：**

- 连接状态：connector `setCallback` → `setConnectState` → `SettingsManager::set(simuConnect)` →
  `settingChanged` 信号 → StatusBar / PdfView 等。
- 每帧数据：`processDataFrame()` → `set(latitu / longitu / altRelat)`（TempKey 信号）+ `emit dataUpdated()`
  → `PdfView::onDataUpdated`（重绘/居中）、StatusBar（置脏）、未来 enroute。
- 设置流：`SettingsManager::set → settingChanged → DataProvider / PdfView / main_widget / StatusBar /
  main_window / Tree`；`broadcast()` 用同一机制做第二波启动初始化。
- 回放：`EventManage::eventReady → replayDataUpdate → processDataFrame → dataUpdated`，完全信号驱动，
  **回放模式不启动 DataProvider 的轮询定时器**。
- UI 交互：滑条 `valueChanged`、按钮 `clicked`、动作 `triggered` 全是信号槽；缩略图是 QCoro 协程
  完成后信号刷新。

一句话：数据源头只有 DataProvider（1Hz 拉适配器）和 StatusBar（1Hz 合并刷新）两处轮询，
其余链路全部是信号槽推送；回放模式把轮询定时器整个关掉，换成事件调度器驱动。

## 8. 设置键速查表

ConstKey（持久化）：

| 键 | 含义 |
| --- | --- |
| MainWindowGeo / MainWidgetSta / OptionWidgetGeo / spliterSta | 窗口与布局状态（ByteArray） |
| dataSource | 数据源（SimulatorSource int） |
| planeFollowed | 飞机居中跟随 |
| stayFront | 窗口置顶 |
| scaleBarEnable | 显示缩放条 |
| chartFolder / dataFolder / globeFolder | 航图文件夹（多个用 `*` 分隔）/ 映射文件夹 / GLOBE 地形数据文件夹 |
| onlyDisplayPdf | 文件树只显示 PDF |
| tcasRange / infoMode / showThumb | TCAS 范围 / 信息模式 / 缩略图 |
| showTrail / useCalGeoHeading | 显示飞行器航迹 / 使用计算航向 |
| debugStoreData / debugReplayData / debugReplayFile | [Debug] 录制回放数据 / 回放模式 / 回放文件路径 |

TempKey（仅运行期）：

| 键 | 含义 |
| --- | --- |
| isDarkTheme | 暗色主题 |
| affineError / affineQuality | 仿射误差（不可用为 NaN）/ 质量 |
| simuConnect | 模拟器连接状态 |
| latitu / longitu | 本机经纬度 |
| altRelat | 高度：非安卓为 AGL 离地高（无高程/海洋 -500），安卓为绝对高度（ft） |
| pageRotate | 页面旋转角（**注意：只写设置，PdfView 还没消费**） |

## 9. 数据文件与命名约定

- 航图：`<ICAO>-<章节>-<页码>.pdf`（如 `ZUCK-3P-01.pdf`）；ICAO 取文件名前 4 字符。
- 映射：`<dataFolder>/<ICAO>.Tmap`，JSON，顶层键 = 航图基本名。
- 尾流等级：`resources/documents/wtc.json`，ICAO → "L/M/H" 等，程序里取首字符。
- 回放文件（Debug）：文本行 + 二进制块混排——首行 `offsetTimeMs,<偏移ms>`，次行 freq；
  之后每条记录 `<相对ms>,<data|connectState>,<字节数>\n` 后紧跟内容；
  `data` 内容为 `qCompress` 的 QDataStream 字节（相对时间 + "data" + 1408 个 float），
  `connectState` 内容为 `true\n` / `false\n`。录制时自动写到系统临时目录。
- 资源前缀：`/map`（飞机贴图）、`/icon`（应用图标）、`/css`（QSS）、`/toolbar`（SVG）、
  `/trans`（翻译）、`/doc`（wtc.json）。

## 10. 平台特化

| 平台 | 特化点 |
| --- | --- |
| 通用 | `constValue.hpp` 用 `_WIN32 / __ANDROID__ / __APPLE__ / __linux__` 定 `platform` 与 `inMacSandbox` |
| macOS | 文件对话框：条件为 `!inMacSandbox` 时用 `DontUseNativeDialog`（看起来反直觉，见 §13）；`APP_SANDBOX_CONTAINER_ID` 检测；App Bundle 图标 |
| Android | 窗口不设标题+最大化；菜单栏转工具栏（`menu2ToolBar`）；新增「权限 → 获取文件夹权限」菜单；设置即时落盘；Manifest 有 `requestLegacyExternalStorage`、横屏固定 `sensorLandscape`；CMake 设包名/图标/打包目录；状态栏显示绝对高度 Alt 而非 AGL（无高程文件） |
| Windows | MinGW 版本必须 13.x；CMake 自动拷贝 Qt DLL/插件；`-mconsole` 调试入口 |

安卓 2026-08 起新增「权限 → 获取文件夹权限」：`main_window::grantFolderPermission()` 调起系统文件夹
选择器，`persistAndroidTreeUri()` 对返回的 `content://` URI 做 `takePersistableUriPermission` 持久化，
并把路径复制到剪贴板。选文件夹本身仍是 `QFileDialog::getExistingDirectory` 纯路径，
SAF 授权后文件树能否稳定访问仍需实测（见 §13）。

## 11. 构建与发布

- 本地：`cmake -B build -DCMAKE_PREFIX_PATH=<Qt 6.9.3 cmake 路径>` 然后 `cmake --build build -j8`。
  示例（macOS）：`/Users/eyderoe/Qt/6.9.3/macos/lib/cmake`。
- CI：`.github/workflows/` 下有 build-macos / build-windows / build-android 三个 workflow，
  全部 `workflow_dispatch` 手动触发；用 aqt 装 Qt 6.9.3（模块 qtpdf、qtpositioning、qtgrpc），
  vcpkg 装依赖；Windows 用 MinGW 13.1.0，Android 用 NDK r27c + SDK 34/35。
  Android 构建会先校验 qrc 资源是否嵌入 `.so`，再用 workflow 里硬编码的 release keystore
  （`android/keystore/` 有同源证书，口令 123）`--release --sign` 签名，
  产物为 `android-build-release-signed.apk`。
- 版本号规范（TODO）：大功能 +1 首位；小功能/改代码 +1 次位；仅备份 +1 第三位。

## 12. 测试

- `test/` 用 doctest，目前覆盖 stringProcess 和 noaaGlobe；`cmake --build build` 后运行
  `./build/test/unit_tests`。
- 构建产物目录 `build/`、`cmake-build-*` 不入库。

## 13. 已知问题 / 遗留事项（维护备忘）

- Windows 下关闭窗口段错误（Qt 6.9 MinGW 线程退出 TLS 清理缺陷，2026-08 已定位）——详见 §13.1。
- XPlaneUDP 设置 TCAS 后原来的 info 不发了——待复现验证。
- 偶发不更新、局域网/XPlane 在线状态显示混乱——待抓包复现。
- Windows 上 ICAO 提取 / 尾流显示在 Debug、Debug 下调试、Release 行为不一致；航班号正常——
  2026-08-14 已定位为 X-Plane RREF 单客户端约 512 订阅上限，详见 §13.2。
- XPlane 航班号乱飞——与上面同根因（flight_id 超出 512 下标的槽位拿不到数据），详见 §13.2。
- Windows 暗色主题未完全解决（themeColor 有 TODO）。
- `pageRotate` 旋转动作只更新了设置，PdfView 没消费（`pdfRotate` 成员 2026-08 已删除，旋转 PDF 仍未实现）。
- `action_add` 在 UI 里存在但没接线（可能想加"添加文件夹"）。
- `enroute_widget` 是空壳，只存了 DataProvider 指针。
- macOS 文件对话框条件看起来写反：非沙盒（`!inMacSandbox`）才用 `DontUseNativeDialog`，
  沙盒内反而用原生对话框——需确认意图。
- 回放 seek 语义：跳转后 `baseMs` 重新锚定，后续事件按"事件相对时间差"继续播，
  不是按原始绝对时刻（这是刻意的；v2.1.3 起已可通过 `replay_control` 实际操作，行为待验证）。

### 13.1 Windows 下关窗口段错误（Qt 6.9 MinGW 线程退出清理崩溃）

> 2026-08-13 排查结论：**不是业务代码的野指针，是 Qt 6.9 MinGW 工具链的线程退出缺陷**。
> 当时怀疑"应该是 XPlaneUDP 那里的错误"是对的——触发点就在 XPlaneUDP 的 worker 线程。

**结论速览（2026-08-14）**

- Debug 崩：worker 线程回调里的 `qDebug` 是活代码，加上调试器（gdb）把时序放大。
- Release 不崩：`QT_NO_DEBUG_OUTPUT` 裁掉 `qDebug`，且 `debugStoreData` 默认关（`replayData->write` 不执行）。
  **但 Release 开着"保存数据"再连 X-Plane 关窗 / 切源，一样会崩。**
- macOS 不崩：没有 Windows 的 DLL TLS 回调机制。
- Qt 6.10：大概率无效（官方 Windows 工具链仍是 MinGW 13.1，无修复证据）。
- 修复成本：低。把回调里的 `qDebug` / `replayData->write` 移出 worker 线程（queued 派发主线程）即可。

**现象**

Windows（Qt 6.9.3 + MinGW GCC 13.1.0 + CLion/Ninja，Debug）下运行段错误。
**不用等关窗**：启动阶段 `SettingsManager::broadcast()` 重建适配器就会触发 XPlaneUDP 析构，
只要此时 worker 线程已碰过 Qt 就崩（见下"触发时机"）；关窗同样触发。
macOS 上同样代码正常。

**崩溃堆栈**（gdb 多次稳定复现）

```text
#0  Qt6Core.dll  0x...4f2c        mov 0x50(%rbx),%rax；rbx=0，空指针解引用
#1  Qt6Core.dll  TLS 回调[0] 内部（RVA 0x3D5FE0，返回点 0x3D608D）
#2  ntdll!RtlDeactivateActivationContext
#3  ntdll!LdrShutdownThread
#4  ntdll!RtlExitUserThread
#5  msvcrt!_endthreadex
#6  libwinpthread-1.dll  pthread_create_wrapper   ← 该线程是 std::thread
```

崩溃线程就是 XPlaneUDP 构造时创建的 `std::thread worker([this]{ io_context.run(); })`。
#1 被 gdb 符号器显示成 `QSystemLibrary::load`/`z_adler32_combine` 是符号缺失导致的误标，
实际是 Qt6Core.dll 的 TLS 回调（PE 的 .CRT 段回调表在 RVA 0xA5E030，共 3 个）。

**根因**

Windows 上每个 DLL 可注册 TLS 回调，ntdll 在每线程退出（DLL_THREAD_DETACH）时统一调用。
Qt6Core.dll 的 cb[0] 负责清理线程局部数据；崩溃函数（RVA 0x244F10）内部调用
libgcc 的 `__emutls_get_address` 取线程局部对象，此时对象首字段已是空指针 → `[rbx+0x50]` 段错误。
即 MinGW 下 Qt 的 TLS 回调执行时机与 libgcc 模拟 TLS / winpthreads 的收尾顺序冲突。
属已知问题家族：mingw-w64 2020/2024 多次修过 winpthreads 的 TLS 清理顺序
（"Don't run cleanup routines immediately"），Qt 6.9 提交 4578e3f8
（QThread/Win 改用 C++11 `thread_local` 处理 adopted thread）之后 MinGW 构建更容易踩中。

**触发条件**（最小复现验证，与业务无关）

一个 `QCoreApplication` + 一个 `std::thread` 的最小程序即可复现，但**崩溃有时序性**：
直接运行 console 复现程序可能干净退出；在调试器（gdb / CLion Debug）下稳定复现。
用户最初正是"调试下"遇到崩溃——CLion Debug 挂在 gdb 上，时序被放大。
复现程序（纯代码）在 `test/connector/`（repro_debug.cpp / repro_emit.cpp /
repro_qfile.cpp / repro_qdatetime.cpp / repro_wlan.cpp / repro_none.cpp /
repro_settings.cpp / repro_filedate.cpp / repro.cpp）。

**触发因子实测**（2026-08-14，同一份 Qt6Core.dll，gdb 多次）：

| 变体 | 线程里做了什么 | gdb 下崩溃 |
| --- | --- | --- |
| repro_qfile_dbg | `QFile::write` | 4/4 |
| repro_filedate_dbg / _rel | `QDateTime` + `QFile::write`（Release 标志也崩） | 3/3 |
| repro_emit_dbg | `qDebug()` + emit（Debug 标志） | 3/3 |
| repro_emit_rel_qdebug | `qDebug()` + emit（`-O2`，不裁 qDebug） | 3/3 |
| repro_emit_rel | 同上，但加 `-DQT_NO_DEBUG_OUTPUT`（qDebug 变空操作） | 0/3 |
| repro_emit_dbg_emitonly | 只 emit（queued 信号），无 qDebug | 0/3 |
| repro_qdatetime_dbg | 只 `QDateTime::currentMSecsSinceEpoch()` | 0/4 |
| repro_wlan_dbg / _rel | `QProtobufSerializer::deserialize` | 0/3 |

结论：**触发源是 `qDebug()` 和 `QFile::write`（QIODevice 文件写）这类会初始化 Qt
线程局部数据的调用，与优化级别无关**；`emit`（跨线程 queued 信号）、`QDateTime`、
`QProtobufSerializer` 都不触发。崩溃点全部相同（Qt6Core.dll TLS 回调，`mov 0x50(%rbx)`）。

**为什么 Release 不崩（实测推翻了早前口头预判）**

早前曾预判"Release 连着 X-Plane 关窗 / 切源同样会崩"，被 CLion Release 和 GitHub Actions
Release 的实测推翻：两种 Release 都从未崩过。原因是两个前提同时成立：
1. CMakeLists.txt（`target_compile_definitions(... $<$<CONFIG:Release>:QT_NO_DEBUG_OUTPUT>)`，
   约 159 行）给 Release 定义了 `QT_NO_DEBUG_OUTPUT`，`qDebug()` 编译成空操作；
2. `debugStoreData`（保存数据）默认关闭，回调里的 `replayData->write` 不执行
   ——这是另一个触发源（QFile 写，见上表），且 Release 不会裁掉它。

于是 worker 线程回调里实际只剩 `setConnectState` 的 emit（上表证明不触发），
信标到了、回调跑了也不会碰 Qt 线程局部数据 → 干净退出。
GitHub Actions 的 Windows Release 用同一份 CMakeLists，行为一致；
它装的是 niXman 的 MinGW 13.1.0（另一份 winpthreads），但起决定作用的是 `QT_NO_DEBUG_OUTPUT`。

⚠ 推论：**Release 开着"保存数据"（debugStoreData）再连 X-Plane 关窗 / 切源，一样会崩**
（`repro_filedate_rel` 用 Release 标志实测 3/3）。这次"没崩"只是因为默认没开。

**为什么 macOS 不崩**

macOS 没有 Windows 的 DLL TLS 回调机制；线程退出时由 libc++/libunwind 按明确定义的顺序执行
`thread_local` 析构，且 macOS 的 Qt 是 Apple clang 编译、`thread_local` 为原生实现，
没有 libgcc 模拟 TLS 和 winpthreads 参与，整条崩溃路径不存在。
所以"mac 上能跑"只能说明没踩雷，不代表该写法跨平台安全。

**本项目触发点**

- **触发时机（2026-08-13 补充确认：不限于关窗）**——XPlaneUDP 析构（→ `close()` → `worker.join()`
  → 线程退出）有多条路径：
  1. 启动阶段：`main_window` 构造末尾 `SettingsManager::broadcast()` 广播 dataSource →
     `DataProvider::setConnector()`（`src/services/dataProvider.cpp:111`）→ `connector->close()` +
     销毁旧适配器；X-Plane 信标若在启动窗口内到达（回调已在 worker 线程碰过 Qt），即崩。
  2. 运行中切换数据源：走同一条 `setConnector()` 路径。
  3. 关窗：DataProvider / StackedWidget 析构，同一路径。
- XPlaneUDP worker（`include/XPlaneUDP.hpp`，`std::thread worker` 跑 asio `io_context`）；
  连接状态回调 `DataProvider::initSimulateDataConnect()`（`src/services/dataProvider.cpp`）
  在 worker 线程里实际做了：
  - `setConnectState`：更新缓存 + emit（`QSettings` 落盘在 `if constexpr (platform == androidOS)`
    里，Windows 上是死代码）——实测安全；
  - `qDebug()`——**触发源**；Debug 是活代码，Release 被 `QT_NO_DEBUG_OUTPUT` 裁掉；
  - `QDateTime::currentMSecsSinceEpoch()`——实测安全；
  - `replayData->write()`（QFile 写）——**触发源**；仅 `debugStoreData` 开启时执行，
    Debug / Release 都会触发。
- wlanUdp worker（`src/connector/wlan.cpp`）：`receiveDataProcess` 里的
  `QProtobufSerializer::deserialize` 实测安全（gdb 0/3）；wlan 的风险与 XPlane 完全相同，
  只来自上面那个共享回调（`setState` → callback）。

**修复方向**（截至本文未实施）

1. 推荐：让 worker 线程全程不碰 Qt——把回调逻辑（`setConnectState` + `qDebug` +
   `replayData->write`）用 `QMetaObject::invokeMethod` / queued 信号派发到主线程执行。
   最小改动：至少把两个触发源（`qDebug` 和 `replayData->write`）移出 worker 线程；
   XPlane 与 WLAN 共用这一个回调，改一处即可。改完在 Windows Debug 下连 X-Plane 关窗验证。
2. 备选：worker 改用 `QThread`（未验证）；或整体换 Qt 6.8.x / llvm-mingw / MSVC 工具链。
3. Qt 6.10：大概率无效。官方 Windows 支持的工具链仍是 MinGW-w64 13.1，winpthreads 的
   TLS 清理顺序补丁未确认进入 Qt 自带工具链，也没有证据修复了本缺陷；若日后升级，
   直接用 `test/connector/repro_debug.cpp` 在 gdb 下验证即可。

### 13.2 X-Plane 的 `icao_type` 拿不到数据：RREF 单客户端约 512 个订阅上限

**现象（2026-08-14 直连运行中的 X-Plane 实测定位）**

- Windows 上飞机 ICAO 提取 / 尾流等级在 Debug、Debug 下调试、Release 三者行为不一致；Mac 一直正常。
- 航班号（flightId）一直正常，ICAO 要么为空（回退显示 `()`），要么是散落的单字符；且每次启动程序，
  `icao_type` 返回的散落字节位置都不一样。

**实测过程（探针在 `debug_probe/`，用 `XPlaneUdp` 直接订阅 TCAS dataref）**

1. 完整订阅 8 组 dataref（id/lat/lon/alt/trk/vs 各 64 + flightId/icao 各 512）：
   `flight_id` 解码完全正常（如 CES7303），`icao_type` 几乎全 0，只有零星几个字节。
2. 只订阅 `icao_type`(下标 0–511) + `flight_id`(512–1023) + `nav1_nav_id`(1024–1523)：
   `icao_type` 完美解码（A20N / B738 / A346 / MD11...），但这次轮到 `flight_id` 坏——正好镜像。
3. 判别实验：`icao_type`(0–511) + `modeS_id`(512–575) + `lat`(576–639)：
   `icao_type` 完美；`modeS_id` 与 `lat`（下标 ≥512）**全 0**。

**结论 / 根因**

X-Plane 的 RREF 订阅按客户端（socket 端点）只响应约前 512 个（下标 <512），超出部分静默不返回
（读到全 0）。不是解码问题——`slice` 对干净的字节数组解码是正确的。

应用现在的下标分配（`XPlaneUdp::findSpace` 按注册顺序分配）：

| dataref | values 下标 | 是否落在 512 内 |
| --- | --- | --- |
| id / lat / lon / alt / trk / vs | 0–383 | ✓ |
| flightId | 384–895 | 仅前 16 个槽（384–511）✓，槽 16+ ✗ |
| icao | 896–1407 | ✗ 全部拿不到 |

所以 `icao_type` 永远全 0 → 尾流查询失败 → 回退显示 `()`；`flightId` 前 16 架正常、超出槽位也坏——
这同时解释了 TODO 里老问题"XPlane 航班号乱飞"。

**为什么构建之间不一致**

1408 个订阅请求是一股脑发出去的，UDP 到达顺序/时序随构建、gdb 挂载、优化级别变化；X-Plane 实际
接受的前 512 个请求具体覆盖到哪些元素不固定 → 表现随构建漂移。Mac 版本/时序不同，恰好没踩中。

**修复方向（探针已验证可行性，未实施）**

把三组订阅拆到三个独立 `XPlaneUdp` 实例，每组下标都从 0 开始且 ≤512：

- 客户端 A：数值数组 6×64 = 384
- 客户端 B：`flight_id` 512
- 客户端 C：`icao_type` 512

涉及 `xpAdapter` 持有多个 `XPlaneUdp`（三个 worker 线程），改动未实施；拆分后需在 Windows 三种
构建下连 X-Plane 验证。

**遗留**

- 上限是实测行为（512 内正常、≥512 无响应），未找到官方文档说明。
- `xp_split_probe.cpp`（拆分验证）未跑完；`xp_live_probe.cpp` / `xp_icao_only_probe.cpp` /
  `xp_cap_probe.cpp` 已验证上述结论。

## 14. 代码风格约定（给未来的自己）

- 注释用中文；头文件守卫 `CHARTNAVIGATION_XXX_HPP`。
- 函数名 `camelCase`，类名 `PascalCase`；声明风格 `void foo () const`（括号前空格）。
- include 优先相对 `src/` 的路径（如 `services/settingManage.hpp`）。
- 业务数据不进 UI 容器；子控件不反向查找父对象，数据靠注入/信号。
- 改完记得同步更新本文档（§6/§7 是最容易过期的部分）。
